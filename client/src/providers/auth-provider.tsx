// ESTE ARQUIVO NÃO É MAIS A FONTE PRINCIPAL DE AUTH

// Reexportamos tudo de @/hooks/use-auth para resolver a dependência circular
export { AuthContext, AuthProvider, useAuth } from "@/hooks/use-auth";